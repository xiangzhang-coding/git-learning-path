# Chapter 1 — Local Basics

The principle thread of this chapter: **snapshots and SHA**. Every command in this chapter moves content between the three areas.

<StageProgress
  :lessons="[
    { text: '1-1 git init and git status', path: '/stage/1/1-1-init-status' },
    { text: '1-2 git add and git commit', path: '/stage/1/1-2-add-commit' },
    { text: '1-3 git log and git diff', path: '/stage/1/1-3-log-diff' },
    { text: '1-4 git restore, git rm and git mv', path: '/stage/1/1-4-restore-rm-mv' }
  ]"
/>

- 1-1: git init creates a repository, git status reads the state
- 1-2: git add stages, git commit saves snapshots
- 1-3: git log views history, git diff views changes
- 1-4: git restore discards, git rm deletes, git mv moves

The playground at the end of each lesson accepts real commands, and task exercises check your result automatically.
